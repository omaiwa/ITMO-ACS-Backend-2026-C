# Local development port mappings — use with:
# docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build

services:
  db:
    ports:
      - "15432:5432"

  rabbitmq:
    ports:
      - "5672:5672"
      - "15672:15672"
